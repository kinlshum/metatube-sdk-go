# MetaTube Admin deployment log

Append one immutable row for every production deployment attempt. A release
can have more than one deployment row. Do not rewrite previous rows; add a
correction row when needed.

| Deployed at (UTC) | Release | Source commit | Image digest | Admin page hash | Scope and preserved services | Live verification / rollback |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-19 19:35 | `custom-2026.09.19.1` | `91586cb` | `sha256:a3209f6b6cb8196005809962244f72a1bf87ddfe70c7699ff26ee012e99aa3cc` | `2d61d962…` | Recreated only `metatube`; preserved Postgres, FlareSolverr, provider bridge, configuration, and `traces.db` | LAN/public Admin and APIs healthy; video/actor expand-collapse verified; roll back to the preceding image recorded by the host if needed |
| 2026-09-19 20:12 | `custom-2026.09.19.2` (superseded build) | `b6d70d8` | `sha256:ceaa677afe3e8e80b8171fc23aca4b6abd44911daad5c3a1e7bcad5a98ef7a4a` | `7b2335d9…` | Recreated only `metatube`; preserved Postgres, FlareSolverr, provider bridge, configuration, and `traces.db` (10.6 MB, untouched) | Page hash matched the pushed source and `/admin/api/trace-stats` reported `ingestion.status=ok`; superseded because the headless-Chrome run then reproduced a run/step tree `TypeError` and a duplicated trace-level error. Rollback: `kinlshum/metatube-server-providers:rollback-20260919-2015` (`sha256:a3209f6b…`, release `.1`) |
| 2026-09-19 20:22 | `custom-2026.09.19.2` | `5af8278` | `sha256:0129e6d41e6c522e17b3e7c02cec572a20940aa6f83564414ad5c2f4b945908c` | `2fb1ccd4…` | Recreated only `metatube` (started `2026-09-19T20:22:36Z`); preserved Postgres, FlareSolverr, provider bridge, configuration, and `traces.db` | LAN and public Admin page hashes equal the pushed `route/admin.html`; error index, `data-event-key`, corrected step window, and summary de-duplication present in the served page; 18/18 headless-Chrome checks passed (single failure, multi failure, zero-error, summary-to-event focus, collapse/reopen, refresh while open, auto-expand, both tabs, no page errors). Rollback: `…:rollback-20260919-2025` (`sha256:ceaa677a…`, superseded build) or `…:rollback-20260919-2015` (`sha256:a3209f6b…`, release `.1`) |
